# SL4A_TouchScreen — one isolated GET_FEATURE after normal HID startup.
# SET, retry, recovery writes, and beta input are disabled in the candidate.
options spi_hid raw_mode=N raw_capture_only=Y raw_input_beta=N raw_transition_once=Y debug_level=2
options spi_amd debug_trace=0
