# SL4A_TouchScreen — one isolated GET/SET observation boot.
# The harness allows exactly one controller-correct SET, then observes only.
options spi_hid raw_mode=N raw_capture_only=Y raw_input_beta=N raw_transition_once=N isolated_set_test=Y debug_level=2
options spi_amd debug_trace=1
